%h1
  Invite people

- form_tag send_invitations_night_path(@night) do
  %p
    %label{ :for => 'night[invitation_emails]' }
      Email addresses:
    = text_area_tag 'night[invitation_emails]'

  = submit_tag "Send Invites!"
