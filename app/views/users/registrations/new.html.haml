%h1 Sign up
-@user.password = @user.password_confirmation = nil

= error_messages_for :user

=form_for @user, :url => user_registration_path do |f|

  = render 'users/form', :f => f, :current_password_required => false

  .submit
    = submit_tag "Sign up"
